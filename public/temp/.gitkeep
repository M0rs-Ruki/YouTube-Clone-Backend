<!-- .gitkeep is a placeholder file used to force Git to track otherwise empty directories. Git by default does not track empty folders, so if you want to keep a directory (like uploads/, logs/, or temp/) in your repo even though it's currently empty, you add a .gitkeep file inside it. -->